name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: CD to back-end
      run: cd back-end/
    - name: Install modules
      run: npm install
    - name: Run ESLint
      run: npx eslint .